# v17.1 Reference Parity Matrix

Reference source note: direct ZIP inspection was attempted, but the three requested ZIPs are not present in this environment. The reference behavior column is therefore based on the user-provided authoritative v15/v15.1 behavior list and existing repository parity inventories; rows marked **ZIP verification required** must be rechecked against extracted artifacts before code repair is finalized.

| Area | Reference behavior | Current RC1 behavior | Root cause | Responsible files | Planned fix | Required regression test |
|---|---|---|---|---|---|---|
| Homepage primary feature | Live radar/map is the primary homepage feature above operational support content. **ZIP verification required.** | Radar appears as the final section after dashboards, risk guide, forecasts, and social content. | Homepage template order was changed to dashboard-first and radar was demoted/lazy-loaded. | `front-page.php`, `template-parts-studio-home.php`, `style.css` | Rebuild homepage shell with radar/map hero first, preserving emergency/live broadcast behavior where appropriate. | Template assertion that `.r9-radar` or map hero appears before decision/current/forecast sections; visual smoke screenshot. |
| Homepage right sidebar | Operational homepage includes a right sidebar with alerts, operations, outage, and quick-link modules. **ZIP verification required.** | No homepage right sidebar or `get_sidebar()` call; only full-width stacked sections. | `front-page.php` delegates to a single full-width partial; `sidebar.php` is missing. | `front-page.php`, `template-parts-studio-home.php`, `sidebar.php`, `functions.php`, `inc/widgets.php` | Add reusable sidebar template and render a right rail on the homepage with widget fallback content. | Public surface validator confirms sidebar file and homepage sidebar call/fallback. |
| Power Outage Tracker | Homepage/public operations expose a Power Outage Tracker. **ZIP verification required.** | No homepage outage module; only slug inventory expects `outage-tracker`. | Outage tracker was omitted from the homepage partial and sidebar defaults. | `template-parts-studio-home.php`, `page.php`, `functions.php`, `style.css` | Restore outage card/section and public page content route with safe empty state. | Validator searches homepage/page templates for outage tracker markup and `/outage-tracker/` link. |
| Alert Center sidebar | Alert Center includes a dedicated sidebar with live alert context, radar, safety/actions, and related products. **ZIP verification required.** | `assets/js/studio.js` renders alert detail panels client-side; page template uses generic forecast sidebar. | Alert Center composition is not separated from generic page layout and no alert sidebar fallback is invoked by slug. | `page.php`, `assets/js/studio.js`, `functions.php`, `inc/widgets.php` | Add alert-specific sidebar rendering for `alerts`/alert detail and keep JS panels progressive-enhancement only. | Slug-based template test for `r9-alert-sidebar` on alert pages and JS no-regression check. |
| Weather Operations sidebar | Weather Operations is a persistent sidebar/rail. **ZIP verification required.** | Weather Operations is a compact card beside the risk guide, not a right sidebar. | Operations content was embedded in a specific homepage row. | `template-parts-studio-home.php`, `inc/operations-bar.php`, `style.css` | Move/add operations module to reusable sidebar while optionally keeping homepage explainer. | Markup order test verifies Weather Operations in sidebar rail. |
| Forecast graphics/layout | Forecast discussion pages include graphics plus discussion and sidebar. | `page.php` includes media placeholder, discussion, key info, and forecast sidebar fallback. | Mostly intact; requires comparison with v15 exact hierarchy. | `page.php`, `functions.php`, `inc/live-studio-integration.php`, `style.css` | Preserve existing fallback while aligning v15 class names/order after ZIP verification. | `scripts/validate-rc1-public-surface.php`; screenshot for key forecast page if app harness exists. |
| Forecast Production workspace | Successful validation populates a visible workspace with generated drafts and actionable controls. | Scheduler calls workspace refresh; admin displays workspace or published products, but batch controls are disabled and submenu is not dedicated. | Workspace generation exists but UI flow is placeholder/disabled and not routed per submenu. | `class-scheduler.php`, `class-product-generator.php`, `class-admin.php` | Create dedicated Forecast Production page with enabled nonce actions tied to workspace rows. | Unit/integration test validates manual validation creates `r9ls_forecast_production_workspace.products` and admin action names exist/enabled. |
| Generation Duration | Duration shows actual validation/generation time. | Admin reads `r9ls_product_generation_last.duration`; symptom can be N/A if workspace refresh not invoked or option missing. | Duration storage is split between scheduler last validation and generator last generation with no fallback. | `class-admin.php`, `class-product-generator.php`, `class-scheduler.php` | Use workspace `duration`, generator duration, then scheduler duration fallback; show `Not run yet` only when absent. | Test fixture invokes validation and asserts duration is numeric in option and rendered label. |
| Decision History | Operator decisions appear after approve/reject/publish/rollback activity. | History only records material-change queue decisions, not workspace approvals/publications. | Workspace actions are not connected to `Material_Change_Engine` history or product publication history. | `class-material-change-engine.php`, `class-product-generator.php`, `class-admin.php` | Introduce workspace decision records and merge queue/workspace histories on Decision History page. | Action test approves/rejects workspace row and asserts `r9ls_decision_history` entry. |
| Approval/publication progression | Products can move from generated draft to approved to published canonical state. | `publish()` writes `r9ls_last_publish` only; no canonical state promotion or generation from approved workspace. | Approval engine and product generator are decoupled. | `class-material-change-engine.php`, `class-product-generator.php`, `class-admin.php`, `class-rest-api.php` | Add service method to approve workspace rows, update approved state, call generator, invalidate public caches. | End-to-end fixture: validate → approve → publish → REST product visible with `approved/published`. |
| Admin menus | Each admin submenu opens a distinct complete dashboard. | All submenu pages call the same monolithic `page()` renderer. | `menu()` reuses one callback without routing. | `class-admin.php` | Route by slug or assign dedicated render methods per submenu. | Static test asserts registered slugs have corresponding render methods/branch cases. |
| Alert Center admin | Admin Alert Center shows canonical alerts, source health, and controls/history. | Alert Center submenu displays the generic dashboard. | No dedicated admin render method. | `class-admin.php`, `class-national-guidance.php`, `class-rest-api.php` | Add Alert Center admin page using `r9ls_canonical_alert_state` and guidance health. | Static and fixture test for alert state table on `r9ls-alert-center`. |
| Version metadata | Theme/plugin metadata consistently reflects target v17.1 repair line. | Theme constant is `4.0.0`; plugin is `17.0.0-rc.1`; docs mention alpha/RC variants. | Metadata was not normalized during RC1 hardening. | `functions.php`, `style.css`, `region9-live-studio.php`, docs | Update only during implementation phase, not in audit commit, per instruction not to update release metadata now. | Static metadata check once implementation phase explicitly permits it. |
| GeneratePress compatibility | Child theme remains compatible with GeneratePress parent. | Parent stylesheet enqueue and child theme header remain present. | No current break identified. | `functions.php`, `style.css`, `header.php`, `footer.php` | Keep repairs in child templates/hooks and avoid parent overrides. | Static check for `Template: generatepress` and parent enqueue. |
